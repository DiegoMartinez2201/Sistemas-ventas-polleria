@model capa.entFormaEnvio

@{
    ViewData["Title"] = "Registrar Forma de Envío";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="container d-flex justify-content-center align-items-center" style="min-height: 80vh;">
    <div class="card shadow-lg p-4" style="max-width: 500px; width: 100%;">
        <div class="card-body">
            <h2 class="card-title text-center mb-3 font-weight-bold">Registrar Nueva Forma de Envío</h2>
            <p class="text-center text-muted mb-4">Completa los datos para agregar una nueva forma de envío al sistema.</p>
            <form asp-action="InsertarFormaEnvio" autocomplete="off">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <!-- Si el idFormaEnvio es autoincremental, puedes ocultar este campo -->
                @* <div class="form-group mb-3">
                    <label asp-for="idFormaEnvio" class="control-label"></label>
                    <input asp-for="idFormaEnvio" class="form-control" placeholder="ID de la Forma de Envío" />
                    <span asp-validation-for="idFormaEnvio" class="text-danger"></span>
                </div> *@
                <div class="form-group mb-3">
                    <label asp-for="nombreForma" class="control-label"></label>
                    <input asp-for="nombreForma" class="form-control" placeholder="Nombre de la Forma de Envío" />
                    <span asp-validation-for="nombreForma" class="text-danger"></span>
                </div>
                <!-- Estado por defecto a 1 (Activo) -->
                <input type="hidden" asp-for="estado" value="1" />
                <div class="form-group text-center">
                    <input type="submit" value="Registrar" class="btn btn-primary btn-lg w-100" />
                </div>
            </form>
            <div class="text-center mt-3">
                <a asp-action="ListarFormaEnvio" class="btn btn-link">Volver a la lista</a>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

