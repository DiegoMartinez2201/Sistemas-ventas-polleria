@model capa.entMetodoDePago

@{
    ViewData["Title"] = "EditarMetodoDePago";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="container d-flex justify-content-center align-items-center" style="min-height: 80vh;">
    <div class="card shadow-lg p-4" style="max-width: 500px; width: 100%;">
        <div class="card-body">
            <h2 class="card-title text-center mb-3 font-weight-bold">Editar Método de Pago</h2>
            <p class="text-center text-muted mb-4">Modifica los datos del método de pago seleccionado.</p>
            <form asp-action="EditarMetodoDePago">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group mb-3">
                    <label asp-for="idMetodoPago" class="control-label"></label>
                    <input asp-for="idMetodoPago" class="form-control" readonly />
                    <span asp-validation-for="idMetodoPago" class="text-danger"></span>
                </div>
                <div class="form-group mb-3">
                    <label asp-for="nombreMetodo" class="control-label"></label>
                    <input asp-for="nombreMetodo" class="form-control" placeholder="Nombre del Método de Pago" />
                    <span asp-validation-for="nombreMetodo" class="text-danger"></span>
                </div>
                <div class="form-group text-center">
                    <input type="submit" value="Guardar Cambios" class="btn btn-primary btn-lg w-100" />
                </div>
            </form>
            <div class="text-center mt-3">
                <a asp-action="ListarMetodoDePago" class="btn btn-link">Volver a la lista</a>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
